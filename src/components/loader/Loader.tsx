import React from "react";
import { LoaderIcon, LoaderIconContainer } from "./loader.styles";

const Loader = () => (
  <LoaderIconContainer>
    <LoaderIcon />
  </LoaderIconContainer>
);

export default Loader;
